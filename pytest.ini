[pytest]
addopts = -v -s --tb=short --color=yes -p no:cacheprovider
testpaths = tests
python_files = test_*.py
python_functions = test_*
# Log capture — narrate() uses logging.info() so steps appear in the HTML report
log_level = INFO
log_format = %(levelname)-8s %(name)-20s %(message)s
